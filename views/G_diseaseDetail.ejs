<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
body {font-family: Arial, Helvetica, sans-serif;}
* {box-sizing: border-box;}

input[type=text], select, textarea {
  width: 100%;
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box;
  margin-top: 6px;
  margin-bottom: 16px;
  resize: vertical;
}

input[type=button] {
  background-color: #4CAF50;
  color: white;
  padding: 12px 20px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

input[type=button]:hover {
  background-color: #45a049;
}

.container {
  border-radius: 5px;
  background-color: #f2f2f2;
  padding: 20px;
}
</style>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
$(function(){
    $($("input[type='button']")[0]).click(function(){
        location.href="/disease/list";
    });
    $($("input[type='button']")[1]).click(function(){
        if(confirm("수정하시겠어요?")){
        $("form").attr({
          "method":"post",
          "action":"/disease/edit"
        });
        $("form").submit();
        }
    });
    $($("input[type='button']")[2]).click(function(){
      if(confirm("삭제하시겠어요?")){
        $("form").attr({
          "method":"post",
          "action":"/disease/delete"
        });
        $("form").submit();
        }
    });
});
function checkValue(){
  if($("form input[name=]").val()==""){

  }else if($("form input[]").val()==""){

  }else{
    
  }
}
</script>
</head>
<body>

<h3>질병 정보 보기</h3>

<div class="container">
  <form>
    <input type="hidden" name="disease_id" value="<%=disease.disease_id%>"/>
    <input type="hidden" name="animal_id" value="<%=animal.disease_id%>"/>
    <input type="hidden" name="div_id" value="<%=div.disease_id%>"/>
    <label>이름</label>
    <input type="text" name="disease_name" value="<%=disease.disease_name%>">
    <label>증상</label>
    <input type="text" name="disease_condition" value="<%=disease.condition%>">
    <label>예방법</label>
    <textarea name="disease_preventing" cols="30" rows="10"><%=disease.disease_preventing%></textarea>

    <input type="button" value="목록으로">
    <input type="button" value="수정하기">
    <input type="button" value="삭제하기">
  </form>
</div>

</body>
</html>
